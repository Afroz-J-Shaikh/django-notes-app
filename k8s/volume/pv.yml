apiVersion: v1
kind: PersistentVolume
metadata:
  name: my-pv
  namespce: app
spec:
  Capacity:
    storage: 1Gi
  accesModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  hostPath:
    path: /mnt/data
