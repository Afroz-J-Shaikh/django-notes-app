apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc
  namespace: app
spec:
  accessModes:
    - ReadWriteMany
  resources:
    request:
      storage: 1Gi
  storageClassName: local-storage 
